#include <iostream>
#include <stdlib.h>

//Define the calss Module
//-It will contain a field called name(string) PRIVATE
//-and a field called numIterations(unsigned shport) PRIVATE
//-A constructor with two param (for the name and numIterations)
//-A method to update that takes the current iteration as a parameter
//and prints its name if the current iteration is less than numIteration
//-Create a main program that defines an array of a few modules 
//"input",5
//"Render",7
//"Logic",10
//and performs a loop of 10 iterations executing their update method.

using namespace std;